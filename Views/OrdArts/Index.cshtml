@model IEnumerable<Pizzeria.Models.OrdArt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().Articoli.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().Ordini.Indirizzo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Articoli.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ordini.Indirizzo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Ordine_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.Ordine_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Ordine_ID })
            </td>
        </tr>
    }

</table>